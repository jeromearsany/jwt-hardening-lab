# --- JWT Configuration ---
# Use a strong, randomly generated secret for signing tokens
# Example generator: openssl rand -base64 32
JWT_SECRET=your_super_strong_secret_goes_here

# --- Token Lifetime ---
ACCESS_TOKEN_LIFETIME=15m
REFRESH_TOKEN_LIFETIME=7d

# --- Token Issuer and Audience ---
TOKEN_ISSUER=https://api.yourapp.com
TOKEN_AUDIENCE=https://yourapp.com
```4.  Now, create the second file. Right-click in the file explorer again and choose **"New File"**.